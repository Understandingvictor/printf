#ifndef MAIN_H
#define MAIN_H
#include <unistd.h>
#include <string.h>
#include <stdarg.h>
int write_string(char *s);
int _printf(const char *format, ...);
int printInt(int num);
int _putchar(char c);
#endif
