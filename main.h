#ifndef MAIN_H
#define MAIN_H
int _putchar(char c);
int printInt(int num);
int _printf(const char *format, ...);
int write_string(char *s);
#include <unistd.h>
#include <stdarg.h>
#include <string.h>

#endif
